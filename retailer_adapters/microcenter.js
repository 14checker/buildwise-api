const { createAdapter } = require("./generic");

module.exports = createAdapter({
  retailerId: "ret-microcenter",
  name: "Micro Center",
  queryName: "Micro Center",
  domains: ["microcenter.com"]
});
